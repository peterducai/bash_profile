# GIT update/pull all repos in directory
alias gitupall='ls | xargs -I{} git -C {} pull'
alias gitshowurls='find . -type d -exec sh -c \'cd "{}"; git config --get remote.origin.url\' \; -prune -print'
